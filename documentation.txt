## 📘 مستندات کامل پلاگین EzLens Secure Login

**نسخه:** 3.0.0  
**نویسنده:** افشین عزتی  
**وب‌سایت:** https://ezlens.ir  
**تاریخ انتشار:** ۱۴۰۴/۰۶/۰۲

---

## 📌 فهرست مطالب

1. [معرفی پلاگین](#۱-معرفی-پلاگین)
2. [معماری و ساختار کلی](#۲-معماری-و-ساختار-کلی)
3. [ساختار فایل‌ها و پوشه‌ها](#۳-ساختار-فایل‌ها-و-پوشه‌ها)
4. [شرح کامل کلاس‌ها](#۴-شرح-کامل-کلاس‌ها)
5. [جداول دیتابیس](#۵-جداول-دیتابیس)
6. [شورت‌کدها](#۶-شورت‌کدها)
7. [هوک‌ها و فیلترها](#۷-هوک‌ها-و-فیلترها)
8. [نکات امنیتی](#۸-نکات-امنیتی)
9. [راهنمای توسعه](#۹-راهنمای-توسعه)
10. [عیب‌یابی](#۱۰-عیب‌یابی)

---

## ۱. معرفی پلاگین

### ۱.۱. چیستی پلاگین

**EzLens Secure Login** یک پلاگین جامع و یکپارچه برای مدیریت **احراز هویت، پنل کاربری، سیستم تیکت پشتیبانی، کمپینگ تبلیغاتی و داشبورد مدیریت** در سایت‌های وردپرسی است. این پلاگین به‌صورت ویژه برای **فروشگاه‌های حوزه سلامت بینایی (عینک، لنز، خدمات چشم‌پزشکی)** طراحی شده اما قابلیت استفاده در هر نوع سایت وردپرسی را دارد.

### ۱.۲. ویژگی‌های کلیدی

| # | ویژگی | توضیح |
|---|-------|--------|
| ۱ | **تغییر مسیر ورود** | جایگزینی `wp-login.php` و `wp-admin` با آدرس‌های سفارشی |
| ۲ | **سه روش ورود** | ورود با OTP (پیامک)، ورود دستی (شماره موبایل + رمز)، ثبت‌نام |
| ۳ | **پنل کاربری کامل** | پیشخوان، سبد خرید، سفارش‌ها، آدرس، پروفایل، نسخه پزشکی، امنیت، پشتیبانی |
| ۴ | **سیستم تیکت پشتیبانی** | چت آنلاین، ارسال فایل، مدیریت تیکت‌ها با صفحه‌بندی و فیلتر |
| ۵ | **کمپینگ تبلیغاتی** | ارسال ایمیل/پیامک گروهی، گروه‌های مخاطبان، Import/Export CSV |
| ۶ | **داشبورد مدیریت** | آمار کاربران، لاگ‌ها، لیست کاربران با شماره تماس |
| ۷ | **سیستم OTP** | کدهای یکبارمصرف ۶ رقمی با انقضا و محدودیت تلاش |
| ۸ | **اتصال به SMS.ir** | ارسال کد تأیید از طریق پیامک |
| ۹ | **ویرایشگر صفحات** | ویرایش HTML/CSS/JS صفحات به‌صورت ایزوله |
| ۱۰ | **بکاپ و API** | خروجی/ورودی JSON، کلید API برای اتصال برنامه‌های خارجی |

### ۱.۳. مخاطبان هدف

- **مدیران فروشگاه‌های آنلاین** (ویژه حوزه سلامت بینایی)
- **توسعه‌دهندگان وردپرس** که به دنبال یک راه‌حل جامع احراز هویت هستند
- **صاحبان کسب‌وکارهای آنلاین** که نیاز به سیستم پشتیبانی و کمپینگ دارند

---

## ۲. معماری و ساختار کلی

### ۲.۱. الگوی طراحی

پلاگین از **الگوی Singleton** برای کلاس‌های اصلی استفاده می‌کند تا از ایجاد چندین نمونه جلوگیری شود.

```php
private static $instance = null;

public static function get_instance() {
    if (null === self::$instance) {
        self::$instance = new self();
    }
    return self::$instance;
}
```

### ۲.۲. ساختار MVC-like

| لایه | مسیر | توضیح |
|------|------|--------|
| **Model** | `includes/` | کلاس‌های مدیریت دیتابیس و منطق |
| **View** | `templates/` | قالب‌های HTML/PHP صفحات |
| **Controller** | `includes/ajax/` | هندلرهای AJAX و `class-admin.php` |
| **Assets** | `assets/` | فایل‌های CSS، JS، Fonts و Icons |

### ۲.۳. جریان بارگذاری

```
ezlens-secure-login.php
    ↓
plugins_loaded → ezlens_auth_load_core_classes()
    ↓
بارگذاری کلاس‌های اصلی (settings, logger, otp, sms, login, ...)
    ↓
is_admin() → بارگذاری class-admin.php
    ↓
init → بارگذاری شورت‌کدها و AJAX
    ↓
wp_enqueue_scripts → بارگذاری Assets (شرطی)
```

---

## ۳. ساختار فایل‌ها و پوشه‌ها

```
ezlens-secure-login/
│
├── ezlens-secure-login.php          # فایل اصلی پلاگین (ورودی)
│
├── assets/                          # فایل‌های استاتیک
│   ├── css/                         # استایل‌ها
│   │   ├── admin.css                # استایل مدیریت (داشبورد، تنظیمات، ویرایشگر)
│   │   ├── frontend-core.css        # استایل‌های پایه فرانت‌اند
│   │   ├── frontend-auth.css        # استایل صفحات ورود/ثبت‌نام
│   │   ├── frontend-panel.css       # استایل پنل کاربری
│   │   ├── frontend-admin.css       # استایل ورود مدیر
│   │   ├── frontend-responsive.css  # استایل ریسپانسیو
│   │   └── support.css              # استایل ویجت چت پشتیبانی
│   │
│   ├── fonts/                       # فونت‌ها
│   │   └── vazirmatn/               # فونت وزیرمتن (داخلی)
│   │       ├── Vazirmatn-Regular.woff2
│   │       ├── Vazirmatn-Medium.woff2
│   │       ├── Vazirmatn-Bold.woff2
│   │       └── Vazirmatn.css
│   │
│   ├── icons/                       # آیکون‌های SVG
│   │   ├── 16/solid/                # آیکون‌های سایز ۱۶
│   │   ├── 20/solid/                # آیکون‌های سایز ۲۰ (Heroicons)
│   │   └── 24/solid/                # آیکون‌های سایز ۲۴
│   │
│   └── js/                          # اسکریپت‌ها
│       ├── admin.js                 # اسکریپت مدیریت (تب‌ها، ویرایشگر، آمار)
│       ├── auth.js                  # اسکریپت احراز هویت (OTP، ورود، ثبت‌نام)
│       ├── panel.js                 # اسکریپت پنل کاربری
│       ├── admin-login.js           # اسکریپت ورود مدیر
│       ├── admin-campaign.js        # اسکریپت کمپینگ
│       └── support.js               # اسکریپت ویجت چت
│
├── includes/                        # کلاس‌های PHP
│   ├── helpers/                     # کلاس‌های کمکی
│   │   └── class-helper.php         # توابع کمکی (نرمال‌سازی، رمزگذاری، ایمیل)
│   │
│   ├── ajax/                        # هندلرهای AJAX
│   │   ├── class-ajax-loader.php    # بارگذار درخواست‌های AJAX
│   │   ├── class-ajax-auth.php      # احراز هویت (ورود، ثبت‌نام، OTP)
│   │   ├── class-ajax-editor.php    # ویرایشگر کد صفحات
│   │   ├── class-ajax-settings.php  # تنظیمات و بکاپ
│   │   ├── class-ajax-tests.php     # تست‌های SMS و SMTP
│   │   ├── class-ajax-support.php   # سیستم پشتیبانی
│   │   └── class-campaign-ajax.php  # سیستم کمپینگ
│   │
│   ├── shortcodes/                  # شورت‌کدها
│   │   ├── class-shortcodes-loader.php  # بارگذار شورت‌کدها
│   │   ├── class-shortcodes-auth.php    # شورت‌کدهای ورود/ثبت‌نام
│   │   ├── class-shortcodes-forgot.php  # شورت‌کد فراموشی رمز
│   │   └── class-shortcodes-panel.php   # شورت‌کد پنل کاربری
│   │
│   ├── class-admin.php              # مدیریت منو و صفحات ادمین
│   ├── class-api.php                # REST API و کلیدهای دسترسی
│   ├── class-campaign.php           # مدیریت کمپین‌ها و گروه‌های مخاطبان
│   ├── class-cron.php               # کرون جاب (پاک‌سازی خودکار)
│   ├── class-logger.php             # لاگ‌گیری ورود/خروج
│   ├── class-login.php              # تغییر مسیر ورود و صفحات سفارشی
│   ├── class-order-meta.php         # متاباکس کد رهگیری سفارش
│   ├── class-order-tracking.php     # رهگیری سفارشات (مراحل و کد رهگیری)
│   ├── class-otp.php                # مدیریت کدهای یکبارمصرف
│   ├── class-settings.php           # مدیریت تنظیمات با کش
│   ├── class-sms.php                # ارتباط با سرویس پیامک SMS.ir
│   ├── class-support.php            # سیستم تیکت پشتیبانی
│   └── class-user-customizations.php # سفارشی‌سازی‌های کاربر (پروفایل، لیست کاربران)
│
└── templates/                       # قالب‌های صفحات
    ├── admin-backup.php             # صفحه بکاپ و API
    ├── admin-dashboard.php          # داشبورد مدیریت
    ├── admin-editor.php             # ویرایشگر کد صفحات
    ├── admin-login.php              # صفحه ورود مدیران
    ├── admin-logs.php               # گزارش‌های لاگ
    ├── admin-settings.php           # تنظیمات عمومی (تب‌دار با AJAX)
    ├── admin-support.php            # صفحه مدیریت پشتیبانی
    ├── admin-campaign.php           # صفحه مدیریت کمپینگ
    ├── lost-password.php            # صفحه فراموشی رمز (غیرفعال)
    ├── minimal-auth.php             # صفحه ورود/ثبت‌نام مشتری
    ├── user-panel.php               # پنل کاربری
    ├── support-chat-widget.php      # ویجت چت پشتیبانی
    ├── campaign-tabs/               # تب‌های کمپینگ
    │   ├── dashboard.php
    │   ├── audience.php
    │   ├── create.php
    │   ├── history.php
    │   └── settings.php
    ├── admin-support-tabs/          # تب‌های پشتیبانی
    │   ├── list.php
    │   ├── conversation.php
    │   └── new-ticket.php
    ├── settings-tabs/               # تب‌های تنظیمات (۱۰ فایل)
    │   ├── general.php
    │   ├── otp.php
    │   ├── sms.php
    │   ├── email.php
    │   ├── smtp.php
    │   ├── captcha.php
    │   ├── appearance.php
    │   ├── security.php
    │   ├── tracking.php
    │   ├── pages.php
    │   └── login-methods.php
    └── defaults/                    # قالب‌های پیش‌فرض برای ویرایشگر
        ├── admin-login.html/css/js
        ├── customer-login.html/css/js
        ├── lost-password.html/css/js
        └── user-panel.html/css/js
```

---

## ۴. شرح کامل کلاس‌ها

### ۴.۱. کلاس‌های اصلی (Core Classes)

#### `EzLens_Auth` (فایل اصلی)
**مسیر:** `ezlens-secure-login.php`  
**وظیفه:** نقطه ورود پلاگین، ثبت هوک‌های فعال‌سازی/غیرفعال‌سازی و بارگذاری Assets.

**متدهای کلیدی:**
- `get_instance()` – دریافت نمونه Singleton
- `activate()` – ایجاد جداول دیتابیس و تنظیمات پیش‌فرض
- `deactivate()` – لغو برنامه‌ریزی کرون
- `enqueue_admin_assets()` – بارگذاری CSS/JS مدیریت
- `enqueue_frontend_assets()` – بارگذاری شرطی CSS/JS فرانت‌اند
- `add_action_links()` – افزودن لینک‌های سریع به صفحه پلاگین‌ها

---

### ۴.۲. کلاس‌های مدیریت (Admin)

#### `EzLens_Auth_Admin`
**مسیر:** `includes/class-admin.php`  
**وظیفه:** مدیریت منوی ادمین، رندر صفحات و ذخیره تنظیمات.

**متدهای کلیدی:**
- `add_admin_menu()` – ثبت منو و زیرمنوها
- `render_dashboard()` – نمایش داشبورد
- `render_settings()` – نمایش تنظیمات
- `render_editor()` – نمایش ویرایشگر صفحات
- `render_support()` – نمایش صفحه پشتیبانی
- `render_campaign()` – نمایش صفحه کمپینگ
- `get_dashboard_stats()` – دریافت آمار با کش
- `get_recent_users_with_phone()` – دریافت کاربران اخیر با شماره تماس

---

#### `EzLens_Auth_Settings`
**مسیر:** `includes/class-settings.php`  
**وظیفه:** مدیریت تنظیمات با کش (`wp_cache`).

**متدهای کلیدی:**
- `get_defaults()` – دریافت مقادیر پیش‌فرض
- `get($key)` – دریافت یک مقدار
- `get_all()` – دریافت تمام تنظیمات (با کش)
- `set($key, $value)` – ذخیره مقدار و پاک‌سازی کش
- `clear_cache()` – پاک‌سازی کش تنظیمات
- `get_page_title($section)` – دریافت عنوان صفحه با کش
- `get_page_subtitle($section)` – دریافت زیرنویس صفحه با کش

---

### ۴.۳. کلاس‌های احراز هویت (Auth)

#### `EzLens_Auth_Login`
**مسیر:** `includes/class-login.php`  
**وظیفه:** تغییر مسیر `wp-login.php`، نمایش صفحات سفارشی و هدایت پس از لاگین.

**متدهای کلیدی:**
- `intercept_login()` – رهگیری درخواست‌های لاگین
- `show_custom_login_page()` – نمایش صفحه ورود سفارشی
- `login_redirect()` – هدایت مشتری به `/my-account/` و مدیر به `admin_url()`
- `on_login()` – ثبت لاگ ورود
- `on_logout()` – ثبت لاگ خروج

---

#### `EzLens_Auth_OTP`
**مسیر:** `includes/class-otp.php`  
**وظیفه:** مدیریت کدهای یکبارمصرف (ذخیره، تأیید، اعتبارسنجی).

**متدهای کلیدی:**
- `create_table()` – ایجاد جدول `wp_ezlens_otp`
- `generate_code()` – تولید کد ۶ رقمی
- `store($mobile, $action)` – ذخیره کد در دیتابیس
- `verify($mobile, $code, $action)` – تأیید کد
- `can_request($mobile, $action)` – بررسی محدودیت درخواست
- `clean_expired()` – پاک‌سازی کدهای منقضی‌شده

---

#### `EzLens_Auth_SMS`
**مسیر:** `includes/class-sms.php`  
**وظیفه:** ارتباط با سرویس پیامک **SMS.ir**.

**متدهای کلیدی:**
- `send_verify($mobile, $code)` – ارسال کد تأیید
- `get_credit()` – دریافت اعتبار حساب
- `is_enabled()` – بررسی فعال بودن پیامک

---

### ۴.۴. کلاس‌های کمپینگ (Campaign)

#### `EzLens_Auth_Campaign`
**مسیر:** `includes/class-campaign.php`  
**وظیفه:** مدیریت کمپین‌ها، گروه‌های مخاطبان و مخاطبان سفارشی.

**متدهای کلیدی:**
- `create_tables()` – ایجاد جداول کمپینگ (۵ جدول)
- `create_campaign($data)` – ایجاد کمپین جدید
- `send_campaign($campaign_id)` – ارسال کمپین (ایمیل/پیامک)
- `create_group($data)` – ایجاد گروه مخاطبان
- `get_group_recipients($group_id)` – دریافت مخاطبان یک گروه
- `add_contact($data)` – افزودن مخاطب دستی
- `import_contacts($csv_data, $headers)` – Import از CSV
- `export_contacts()` – Export به CSV

---

### ۴.۵. کلاس‌های پشتیبانی (Support)

#### `EzLens_Auth_Support`
**مسیر:** `includes/class-support.php`  
**وظیفه:** مدیریت تیکت‌های پشتیبانی و پیام‌ها.

**متدهای کلیدی:**
- `create_tables()` – ایجاد جداول تیکت‌ها و پیام‌ها
- `create_ticket($user_id, $message, $attachment_id)` – ایجاد تیکت جدید
- `add_message($ticket_id, $sender_id, $sender_type, $message)` – افزودن پیام
- `get_tickets($status, $search, $limit, $offset)` – دریافت لیست تیکت‌ها
- `get_messages($ticket_id)` – دریافت پیام‌های یک تیکت
- `update_status($ticket_id, $status)` – تغییر وضعیت تیکت
- `send_notification_email($user_id, $subject, $message)` – ارسال ایمیل به کاربر

---

### ۴.۶. کلاس‌های AJAX

| کلاس | مسیر | وظیفه |
|------|------|--------|
| `EzLens_Auth_Ajax_Auth` | `ajax/class-ajax-auth.php` | OTP، ورود، ثبت‌نام، فراموشی رمز |
| `EzLens_Auth_Ajax_Editor` | `ajax/class-ajax-editor.php` | ذخیره/بازنشانی کدها، پیش‌نمایش، فعال‌سازی صفحات |
| `EzLens_Auth_Ajax_Settings` | `ajax/class-ajax-settings.php` | ذخیره تنظیمات، بکاپ، API Key |
| `EzLens_Auth_Campaign_Ajax` | `ajax/class-campaign-ajax.php` | ایجاد/ارسال/حذف کمپین، گروه‌ها، مخاطبان |
| `EzLens_Auth_Ajax_Support` | `ajax/class-ajax-support.php` | ایجاد تیکت، پاسخ، تغییر وضعیت، جستجوی کاربران |
| `EzLens_Auth_Ajax_Tests` | `ajax/class-ajax-tests.php` | تست SMS و SMTP |

---

### ۴.۷. کلاس‌های کمکی

#### `EzLens_Auth_Helper`
**مسیر:** `helpers/class-helper.php`  
**وظیفه:** توابع کمکی عمومی.

**متدهای کلیدی:**
- `normalize_mobile($mobile)` – نرمال‌سازی شماره موبایل (تبدیل ۰۰۹۸ به ۰۹...)
- `get_user_by_phone($phone)` – جستجوی کاربر با شماره موبایل
- `send_email($to, $subject, $template, $placeholders)` – ارسال ایمیل با قالب
- `encrypt($plain_text)` – رمزگذاری با `openssl_encrypt`
- `decrypt($encrypted_text)` – رمزگشایی
- `check_login_attempts($ip, $limit, $lockout_minutes)` – بررسی Rate Limiting
- `increment_login_attempts($ip, $lockout_minutes)` – افزایش تلاش‌ها

---

## ۵. جداول دیتابیس

### ۵.۱. جدول لاگ‌ها (`wp_ezlens_logs`)

```sql
CREATE TABLE wp_ezlens_logs (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    user_id bigint(20) NOT NULL,
    username varchar(60) NOT NULL,
    action varchar(20) NOT NULL,          -- 'login', 'logout', 'failed_login'
    ip varchar(45) NOT NULL,
    user_agent text,
    timestamp datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY user_id (user_id),
    KEY action (action),
    KEY timestamp (timestamp)
);
```

---

### ۵.۲. جدول OTP (`wp_ezlens_otp`)

```sql
CREATE TABLE wp_ezlens_otp (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    mobile varchar(15) NOT NULL,
    code varchar(6) NOT NULL,
    action varchar(20) NOT NULL DEFAULT 'login',  -- 'login', 'register_email', 'register_phone', 'forgot_sms', 'forgot_email'
    expires_at datetime NOT NULL,
    is_used tinyint(1) NOT NULL DEFAULT 0,
    attempts int(11) NOT NULL DEFAULT 0,
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY mobile (mobile),
    KEY code (code),
    KEY expires_at (expires_at)
);
```

---

### ۵.۳. جداول کمپینگ (۵ جدول)

#### `wp_ezlens_campaigns` (کمپین‌ها)
```sql
CREATE TABLE wp_ezlens_campaigns (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    name varchar(255) NOT NULL,
    type varchar(20) NOT NULL DEFAULT 'email',
    subject varchar(255) DEFAULT '',
    message longtext NOT NULL,
    file_attachment varchar(255) DEFAULT '',
    status varchar(20) NOT NULL DEFAULT 'draft',
    scheduled_at datetime DEFAULT NULL,
    sent_at datetime DEFAULT NULL,
    stats longtext,
    created_by bigint(20) NOT NULL,
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY status (status),
    KEY created_at (created_at)
);
```

#### `wp_ezlens_campaign_contacts` (مخاطبان سفارشی)
```sql
CREATE TABLE wp_ezlens_campaign_contacts (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    name varchar(255) NOT NULL,
    email varchar(100) NOT NULL,
    phone varchar(20) DEFAULT '',
    category varchar(100) DEFAULT 'عمومی',
    extra_fields longtext,
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY email (email),
    KEY category (category)
);
```

#### `wp_ezlens_campaign_groups` (گروه‌های مخاطبان)
```sql
CREATE TABLE wp_ezlens_campaign_groups (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    name varchar(255) NOT NULL,
    description text,
    type varchar(20) NOT NULL DEFAULT 'custom',  -- 'wordpress', 'custom', 'mixed'
    user_filters longtext,
    contact_ids longtext,
    created_by bigint(20) NOT NULL,
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY created_by (created_by)
);
```

#### `wp_ezlens_campaign_group_relations` (ارتباط کمپین با گروه‌ها)
```sql
CREATE TABLE wp_ezlens_campaign_group_relations (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    campaign_id bigint(20) NOT NULL,
    group_id bigint(20) NOT NULL,
    PRIMARY KEY (id),
    KEY campaign_id (campaign_id),
    KEY group_id (group_id)
);
```

#### `wp_ezlens_campaign_tracks` (ترک باز شدن ایمیل)
```sql
CREATE TABLE wp_ezlens_campaign_tracks (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    campaign_id bigint(20) NOT NULL,
    recipient_email varchar(100) NOT NULL,
    opened_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    ip varchar(45) DEFAULT '',
    user_agent text,
    PRIMARY KEY (id),
    KEY campaign_id (campaign_id),
    KEY recipient_email (recipient_email)
);
```

---

### ۵.۴. جداول پشتیبانی (۲ جدول)

#### `wp_ezlens_support_tickets` (تیکت‌ها)
```sql
CREATE TABLE wp_ezlens_support_tickets (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    user_id bigint(20) NOT NULL,
    status varchar(20) NOT NULL DEFAULT 'open',
    subject varchar(255) DEFAULT '',
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY user_id (user_id),
    KEY status (status)
);
```

#### `wp_ezlens_support_messages` (پیام‌ها)
```sql
CREATE TABLE wp_ezlens_support_messages (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    ticket_id bigint(20) NOT NULL,
    sender_id bigint(20) NOT NULL,
    sender_type varchar(10) NOT NULL DEFAULT 'user',
    message text NOT NULL,
    file_attachment varchar(255) DEFAULT '',
    is_read tinyint(1) NOT NULL DEFAULT 0,
    created_at datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY ticket_id (ticket_id),
    KEY sender_id (sender_id)
);
```

---

## ۶. شورت‌کدها

| شورت‌کد | کاربرد | مثال |
|---------|--------|-------|
| `[minimal_auth]` | صفحه ورود/ثبت‌نام مشتری | `[minimal_auth]` |
| `[ezlens_lost_password]` | صفحه فراموشی رمز (غیرفعال) | `[ezlens_lost_password]` |
| `[modern_user_panel]` | پنل کاربری کامل | `[modern_user_panel]` |
| `[admin_login_page]` | صفحه ورود مدیر | `[admin_login_page]` |
| `[ezlens_support_chat]` | ویجت چت پشتیبانی | `[ezlens_support_chat]` |

---

## ۷. هوک‌ها و فیلترها

### ۷.۱. فیلترها (Filters)

| فیلتر | توضیح | نمونه |
|-------|-------|--------|
| `ezlens_auth_login_redirect` | تغییر مسیر پس از ورود | `add_filter('ezlens_auth_login_redirect', function($url, $user) { return home_url('/custom-page/'); }, 10, 2);` |
| `ezlens_auth_register_redirect` | تغییر مسیر پس از ثبت‌نام | `add_filter('ezlens_auth_register_redirect', function($url, $user_id) { return home_url('/welcome/'); }, 10, 2);` |
| `ezlens_auth_customer_redirect` | تغییر مسیر مشتری پس از ورود | `add_filter('ezlens_auth_customer_redirect', function($url, $user) { return home_url('/my-account/'); }, 10, 2);` |

### ۷.۲. اکشن‌ها (Actions)

| اکشن | توضیح | نمونه |
|------|-------|--------|
| `ezlens_auth_user_registered` | پس از ثبت‌نام کاربر جدید | `add_action('ezlens_auth_user_registered', function($user_id, $data) { error_log('User registered: ' . $data['email']); }, 10, 2);` |

---

## ۸. نکات امنیتی

### ۸.۱. Nonce برای تمام درخواست‌ها

```php
// در JS
'nonce' => wp_create_nonce('ezlens_auth_nonce')

// در PHP
check_ajax_referer('ezlens_auth_nonce', 'nonce');
```

### ۸.۲. رمزگذاری کلیدهای حساس

کلیدهای زیر با `openssl_encrypt` رمزگذاری می‌شوند:
- `sms_api_key`
- `smtp_password`
- `captcha_secret_key`

### ۸.۳. Sanitization و Validation

تمام ورودی‌ها با توابع استاندارد وردپرس پاک‌سازی می‌شوند:
- `sanitize_text_field()`
- `sanitize_email()`
- `sanitize_textarea_field()`
- `wp_kses_post()`
- `esc_url_raw()`

### ۸.۴. مسدودسازی `wp-admin` برای کاربران غیرمدیر

```php
if (is_admin() && !wp_doing_ajax() && !defined('DOING_AJAX')) {
    $current_user = wp_get_current_user();
    if (!is_user_logged_in() || !in_array('administrator', (array) $current_user->roles)) {
        $this->show_404();
        exit;
    }
}
```

### ۸.۵. محدودیت تلاش OTP

هر شماره موبایل حداکثر **۵ بار** تلاش ناموفق می‌تواند داشته باشد.

### ۸.۶. Rate Limiting برای ورود

با استفاده از `transient`، تلاش‌های ناموفق ورود محدود می‌شوند (پیش‌فرض: ۵ تلاش در ۱۵ دقیقه).

---

## ۹. راهنمای توسعه

### ۹.۱. افزودن یک شورت‌کد جدید

**مرحله ۱:** در `includes/shortcodes/class-shortcodes-loader.php` شورت‌کد را ثبت کنید:

```php
add_shortcode('my_custom_shortcode', [$this, 'render_my_shortcode']);

public function render_my_shortcode($atts) {
    return '<div>محتوای شورت‌کد</div>';
}
```

**مرحله ۲:** در `ezlens-secure-login.php` شورت‌کد را به لیست بررسی‌ها اضافه کنید:

```php
$shortcodes = ['minimal_auth', 'my_custom_shortcode', ...];
```

---

### ۹.۲. افزودن یک تب جدید به تنظیمات

**مرحله ۱:** ایجاد فایل `templates/settings-tabs/mytab.php`

**مرحله ۲:** در `includes/ajax/class-ajax-settings.php` کیس جدید اضافه کنید:

```php
case 'mytab':
    include EZLAUTH_PLUGIN_DIR . 'templates/settings-tabs/mytab.php';
    break;
```

**مرحله ۳:** در `templates/admin-settings.php` دکمه تب جدید اضافه کنید:

```php
<button class="settings-tab" data-tab="mytab">تب من</button>
```

---

### ۹.۳. افزودن یک متد AJAX جدید

**مرحله ۱:** در کلاس AJAX مناسب، متد جدید اضافه کنید:

```php
public static function my_ajax_action() {
    check_ajax_referer('ezlens_auth_nonce', 'nonce');
    if (!current_user_can('manage_options')) {
        wp_send_json_error('دسترسی غیرمجاز');
        wp_die();
    }
    // منطق شما
    wp_send_json_success(['message' => 'انجام شد.']);
    wp_die();
}
```

**مرحله ۲:** در `init()` اکشن را ثبت کنید:

```php
add_action('wp_ajax_ezlens_my_ajax_action', [__CLASS__, 'my_ajax_action']);
```

**مرحله ۳:** در JS درخواست ارسال کنید:

```javascript
$.post(ezlens_auth_ajax.ajax_url, {
    action: 'ezlens_my_ajax_action',
    nonce: ezlens_auth_ajax.nonce,
    // داده‌ها
}, function(response) {
    // پردازش پاسخ
});
```

---

### ۹.۴. افزودن یک متغیر قابل شخصی‌سازی در ایمیل‌ها

**مرحله ۱:** در متد `send_campaign` یا `send_email`، متغیر جدید را به جایگزین‌ها اضافه کنید:

```php
$message = str_replace(
    ['{name}', '{email}', '{my_custom_var}'],
    [$recipient['name'], $recipient['email'], $custom_value],
    $campaign->message
);
```

**مرحله ۲:** در مستندات، متغیر جدید را معرفی کنید.

---

## ۱۰. عیب‌یابی

### ۱۰.۱. مشکلات رایج و راه‌حل‌ها

| مشکل | راه‌حل |
|------|--------|
| **خطای ۴۰۴ در wp-admin** | مطمئن شوید کاربر نقش مدیر دارد. در غیر این صورت به آدرس جدید ورود هدایت می‌شود. |
| **عدم دریافت کد OTP** | تنظیمات SMS را بررسی کنید (API Key, Line Number, Template ID). حالت Sandbox را غیرفعال کنید. |
| **خطای ۴۰۳ در AJAX** | `nonce` را بررسی کنید. مطمئن شوید `check_ajax_referer` با نام صحیح فراخوانی شده است. |
| **خطای ۵۰۰ در AJAX** | لاگ‌های PHP را بررسی کنید. احتمالاً یک خطای `wp_die()` یا `syntax error` وجود دارد. |
| **تیکت ایجاد نمی‌شود** | جداول دیتابیس را بررسی کنید. متد `create_tables` را در `class-support.php` اجرا کنید. |
| **مخاطب دستی اضافه نمی‌شود** | ستون `category` را در جدول `wp_ezlens_campaign_contacts` بررسی کنید. |
| **کمپین ارسال نمی‌شود** | تنظیمات SMTP و SMS را بررسی کنید. مطمئن شوید گروه‌های مخاطبان انتخاب شده‌اند. |

### ۱۰.۲. فعال‌سازی حالت دیباگ

برای مشاهده خطاها، در فایل `wp-config.php` کد زیر را اضافه کنید:

```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

سپس خطاها در `wp-content/debug.log` ذخیره می‌شوند.

### ۱۰.۳. بازنشانی تنظیمات

برای بازنشانی تمام تنظیمات پلاگین به حالت پیش‌فرض:
- به بخش **تنظیمات عمومی EzLens** بروید.
- روی دکمه **بازنشانی همه** کلیک کنید.

---

## 📌 اطلاعات تماس و پشتیبانی

- **وب‌سایت:** https://ezlens.ir
- **ایمیل:** info@ezlens.ir
- **تلفن:** 02144385667

---

**پایان مستندات**  
**نسخه:** 3.0.0  
**تاریخ:** ۱۴۰۴/۰۶/۰۲